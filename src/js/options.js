import React from 'react';
import ReactDom from 'react-dom';
import OptionsPage from './components/OptionsPage';

const reactRoot = document.getElementById('react');

ReactDom.render(<OptionsPage />, reactRoot);
